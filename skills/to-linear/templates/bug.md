<!--
An `optional` HTML-comment marker under a heading (see below) tells
/linear-groom-ticket's completeness lint that the section is not required. Linear
renders nothing for it and filling this template ignores it. Delete sections from
the ticket you write, never from this file.
-->

## Summary

What is wrong, in the smallest number of sentences that still name the mechanism. Point at the
code with `path/to/file.ext:123` references — they are clickable and they save the next person
the search. If the root cause is not known yet, say so here explicitly rather than implying one.

## Steps to Reproduce

1. …
2. …

If it cannot be reproduced on demand, replace the steps with the evidence that it happened —
a log line, a Sentry issue, a failed pipeline run, an incident number.

## Expected Behavior

## Actual Behavior

## Environment Details
<!-- optional -->

Which environments are affected (prod / client env / local), which modules or services, and
since when if known. Name the release or PR that introduced it if you found it.

## Additional Context
<!-- optional -->

Anything that saves the implementer an afternoon: the trap that is easy to fall into, the
sibling call sites that look related but are not, the reason the obvious fix is wrong, links
to related issues.

<!--
Optional sections used freely — add whichever earn their place:
  ## Root Cause            ## Why nothing caught it     ## Proposed Fix
  ## Suggested approach    ## Verification              ## Affected call sites
  ## Suggested Definition of Done
-->
